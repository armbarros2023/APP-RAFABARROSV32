server {
    listen 80;
    server_name app.clinicarafabarros.com.br;

    # Redirecionar HTTP para HTTPS (será ativado após SSL)
    # return 301 https://$server_name$request_uri;

    # Backend API
    location /api {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # CORS headers
        add_header 'Access-Control-Allow-Origin' 'https://app.clinicarafabarros.com.br' always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
        add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type' always;
        
        # Handle preflight
        if ($request_method = 'OPTIONS') {
            return 204;
        }
    }

    # Frontend
    location / {
        root /var/www/clinicrafabarros;
        try_files $uri $uri/ /index.html;
        
        # Cache static assets
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}

# HTTPS configuration (will be added by Certbot)
# server {
#     listen 443 ssl http2;
#     server_name app.clinicarafabarros.com.br;
#     
#     ssl_certificate /etc/letsencrypt/live/app.clinicarafabarros.com.br/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/app.clinicarafabarros.com.br/privkey.pem;
#     
#     # Same locations as above
# }
